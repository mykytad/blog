<% provide(:title, @user.name) %>
<div class="col d-flex flex-column align-items-start gap-2">
	<h2>
		<!-- <%= gravatar_for @user %> -->
		<%= @user.name %>
	</h2>
	<p class="text-muted"></p>

</div>