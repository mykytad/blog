<h1>Articles</h1>

Our blog has <%= Article.public_count %> articles and counting!

<ul class="list-group">
  <% @articles.each do |article| %>
    <li class="list-group-item">
      <%= link_to article.title, article, class:"list_link" %>
    </li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>